# Motion Examples (336)

> Complete index of all Motion library examples from motion.dev
> Use `scripts/get-example.sh <id> [platform]` to fetch source code

## JavaScript Examples (52)

### Category: Basics

| Example | APIs | Description |
|---------|------|-------------|
| `bounce-easing` Bounce easing | animate | A toggle switch that demonstrates custom easing functions with both bounce an... |
| `color-interpolation` Color interpolation | animate | A comparison of color interpolation between Motion and the browser's native i... |
| `gestures` Gestures | animate, press, hover | An example of using Motion's press and hover functions to animate elements as... |
| `hover` Hover | animate, hover | An example of using Motion's hover function to animate elements as a user hov... |
| `html-content` HTML content | animate | An example using Motion to animate HTML content via the Element.innerText pro... |
| `layered-sections` Layered sections | - | An example of creating a layered scrolling effect using CSS sticky to replace... |
| `lightbox` Lightbox | press, animateView, spring | An example of a lightbox shared element transitionusing Motion's view function. |
| `options` Basic animation | animate | An example of using easing and duration options to customise a scale animatio... |
| `page-wipe` Page wipe | press, animateView, spring | An example of combining a full page wipe with a shared element transition usi... |
| `parallax` Parallax | animate | An example of creating a parallax effect using Motion's scroll function. |
| `press` Press | animate, press | An example of using Motion's press function to animate elements as a user pre... |
| `rotate` Rotate | animate | An example of a rotation animation in Motion. |
| `scroll-fade` Scroll fade in/out | animate | An example of fading content in and out as you scroll using Motion's scroll f... |
| `scroll-pinning` Scroll pinning | animate | An example of pinning content on scroll and animating content horizontally us... |
| `scroll-triggered` Scroll-triggered animation | animate, inView | An example of using Motion's inView function to trigger animations when eleme... |
| `shared-view-animation` Shared view animation | press, animateView, spring | Animate shared elements between states using View Transitions API via Motion'... |
| `spring` Spring | animate | An example of a spring animation in Motion. |
| `spring-follow-cursor` Spring: Follow Cursor | animate, styleEffect | An example of a spring animation that follows the cursor. |
| `stagger` Stagger | animate, stagger | An example of staggering animations in Motion using its stagger() function. |
| `staggered-grid` Physical stagger | animate | An example of animating elements with a delay staggered by physical distance. |
| `svg-loading-spinner` SVG loading spinner | animate, stagger | An example of using Motion to animate an SVG loading spinner. |
| `svg-path-morphing` SVG path morphing | animate | An example of using Motion and Flubber to animate an SVG between different sh... |
| `three` Three.js | animate | An example of a rotating cube using Three.js and Motion. |
| `view-animation` View animation | press, animateView, spring | An example of animating a layout change using the View Transitions API via Mo... |

### Category: Buttons

| Example | APIs | Description |
|---------|------|-------------|
| `hold-to-confirm` Hold to confirm | animate, motionValue, mapValue, styleEffect, press | A Motion example showing long-press interaction. The button's events update a... |
| `material-design-ripple` Material Design: Ripple | animate, hover | An example of creating a press ripple with Motion, inspired by Google Materia... |
| `multi-state-badge` Multi state badge | animate, motionValue, transform, press | A badge that can be in multiple states, such as processing, success, or error. |

### Category: Dialog

| Example | APIs | Description |
|---------|------|-------------|
| `accordion` Accordion | animate, press | An example of an accordion component using Motion. Animate accordion content ... |
| `family-dialog` Family-style dialog | animate, hover, press, spring, animateView | Family-style dialog using Motion. It uses the HTML dialog and Motion's animat... |
| `modal` Modal dialog | animate, hover, press | An example of a modal dialog using Motion and the HTML <dialog /> element. Th... |

### Category: Experimental

| Example | APIs | Description |
|---------|------|-------------|
| `conic-gradient-pointer` Conic gradient pointer | motionValue | An example of creating a dynamic conic gradient that follows the pointer posi... |

### Category: Forms

| Example | APIs | Description |
|---------|------|-------------|
| `characters-remaining` Characters remaining | animate, transform | An example of creating a character counter with spring animations in Motion |
| `ios-slider` iOS slider | animate, mix, motionValue, mapValue, transformValue, styleEffect | An example of recreating the iOS slider using Motion for JavaScript. This exa... |

### Category: Interactions

| Example | APIs | Description |
|---------|------|-------------|
| `image-reveal-slider` Image reveal slider | animate, motionValue, mapValue, styleEffect | An image reveal slider that uses Motion's drag functionality to create a befo... |
| `notifications-list` Notifications list | animate, press | An example of animating a list of animations as they're added and removed, us... |
| `notifications-stack` Notifications stack | animate, press | A iOS inspired notifications stack using Motion's variants. |
| `tilt-card` Tilt card | animate | A card that tilts based on pointer position using Motion's spring animations. |

### Category: Layout

| Example | APIs | Description |
|---------|------|-------------|
| `app-store` iOS App Store | animateView | An example of creating a card layout inspired by the iOS App Store with Modal... |

### Category: Loading

| Example | APIs | Description |
|---------|------|-------------|
| `loading-circle-spinner` Loading: Circle spinner | animate | An example of creating a circle spinner loading animation with Motion. |
| `loading-fill-text` Fill text | animate, transform | An example of creating a text fill loading animation with Motion. |
| `loading-infinite-path-drawing` Loading: Infinite path drawing | animate, motionValue | An example of creating an infinite path drawing loading animation with Motion... |
| `loading-jumping-dots` Loading: Jumping dots | animate, stagger | An example of creating a jumping dots loading animation with Motion. |
| `loading-line-reveal` Loading overlay | animate, transform | An example of creating a line reveal loading animation with clipPath in Motion |
| `loading-progress-bar` Loading progress bar | animate, transform | An example of creating a progress bar loading animation with Motion. |
| `loading-ripple` Loading ripple | animate, stagger | An example of creating a ripple loading animation with Motion. |
| `loading-three-dots-pulse` Loading: Pulse dots | animate, stagger | An example of creating a pulsing dots loading animation with Motion. |

### Category: Scroll

| Example | APIs | Description |
|---------|------|-------------|
| `scroll-highlight` Scroll highlight | inView | An example of creating a scroll-triggered skill showcase with dynamic highlig... |

### Category: Text

| Example | APIs | Description |
|---------|------|-------------|
| `split-text` Split Text | animate, stagger, splitText | Demonstrate how to use the splitText function from Motion+ to create staggere... |
| `split-text-scatter` Split Text: Scatter | animate, hover, splitText | Demonstrate how to use the splitText function from Motion+ to create scatter ... |
| `split-text-wavy` Split Text: Wavy | animate, stagger, hover, splitText | Demonstrate how to use the splitText function from Motion+ to create wavy tex... |
| `text-reveal` Reveal text effect | animate, hover | An example of revealing text on hover using flexbox and Motion's layout anima... |
| `typewriter` Typewriter | animate | An example of a typewriter effect using Motion for JavaScript animation values. |

## React Examples (151)

### Category: Base UI

| Example | APIs | Description |
|---------|------|-------------|
| `base-accordion` Base UI: Accordion | AnimatePresence, motion, MotionConfig, animate, whileTap, variants, layout, layoutId | An example of creating an accordion component with Base UI and Motion for React. |
| `base-checkbox` Base UI: Checkbox | motion, useMotionValue, useTransform, animate, whileHover, whileTap | An example of creating a checkbox with path line animation for the tick using... |
| `base-context-menu` Base UI: Context menu | AnimatePresence, motion, animate | An example of creating a context menu with smooth animations using Base UI an... |
| `base-dialog` Base UI: Dialog | AnimatePresence, motion, animate, whileHover, whileTap | An example of creating a dialog component with overlay and content animations... |
| `base-menu` Base UI: Dropdown menu | AnimatePresence, motion, animate, whileHover, whileTap, whileFocus | An example of creating a dropdown menu with smooth animations using Base UI a... |
| `base-progress` Base UI: Progress | motion, useMotionValue, useTransform, animate | An example of creating a progress component using Base UI and Motion for React. |
| `base-radio` Base UI: Radio | AnimatePresence, motion, useMotionValue, useTransform, animate, whileHover, whileTap | An example of creating a radio group component using Base UI and Motion for R... |
| `base-select` Base UI: Select | AnimatePresence, motion, animate, whileHover, whileTap, viewport | An example of creating a select component using Base UI and Motion for React. |
| `base-switch` Base UI: Switch | motion, animate, whileFocus, layout | An example of creating a switch component using Base UI and Motion for React.... |
| `base-tabs` Base UI: Tabs | AnimatePresence, LayoutGroup, motion, animate, whileTap, layout, layoutId | An example of creating a tabs component using Base UI and Motion for React. I... |
| `base-toast` Base UI: Toast | AnimatePresence, motion, animate, whileHover, whileTap, viewport, drag | An example of creating a toast component using Base UI and Motion for React. ... |
| `base-toggle-group` Base UI: Toggle group | AnimatePresence, motion, animate, whileTap, whileFocus, layout, layoutId | An example of creating a toggle group component using Base UI and Motion for ... |
| `base-tooltip` Base UI: Tooltip | AnimatePresence, motion, animate | An example of creating a tooltip component using Base UI and Motion for React. |

### Category: Basics

| Example | APIs | Description |
|---------|------|-------------|
| `animate-activity` AnimateActivity: Slideshow | motion, animate, whileTap, whileFocus, layout | An example of using AnimateActivity to create a slideshow. |
| `animate-presence-modes` AnimatePresence modes | AnimatePresence, motion, animate, whileTap | An example of the three AnimatePresence modes (sync, wait, and popLayout) dem... |
| `aspect-ratio` Aspect ratio | motion, layout | An example of animating aspect-ratio using Motion for React's layout animations. |
| `bounce-easing` Bounce easing | motion, layout | An example of creating a bounce custom easing function in Motion for React. |
| `color-interpolation` Color interpolation | useAnimate, animate | A comparison of color interpolation between Motion and the browser's native i... |
| `css-spring` CSS spring | spring | An example of generating a spring animation in CSS using Motion's spring func... |
| `drag` Drag | motion, drag | An example of making an element draggable using Motion for React's drag prop. |
| `drag-constraints` Drag: Constraints | motion, drag | An example of making an element draggable using Motion for React's drag prop.... |
| `drag-lock-direction` Drag: Lock direction | motion, animate, drag | An example of locking the drag direction of an element based on its initial d... |
| `enter-animation` Enter animation | motion, animate | An example of animating an element when it's added to the DOM using Motion fo... |
| `exit-animation` Exit animation | AnimatePresence, motion, animate, whileTap | An example of animating an element when it's removed from the DOM using Anima... |
| `follow-pointer-with-spring` Follow pointer with spring | motion, useSpring | An example of making an element follow the pointer with a spring using Motion... |
| `gestures` Gestures | motion, whileHover, whileTap | An example of using gestures to animate an element using Motion for React's s... |
| `html-content` HTML content | motion, useMotionValue, useTransform, animate | An example using Motion for React to animate HTML content by rendering a Moti... |
| `keyframes` Keyframes | motion, animate | An example of animating an element using keyframes with Motion for React. |
| `keyframes-wildcards` Keyframe wildcards | motion, whileHover | An example of using Motion for React's keyframe wildcards to create interrupt... |
| `layout-animation` Layout animation | motion, layout | An example of animating a layout change using Motion for React's simple layou... |
| `motion-path` Motion along a path | motion, animate | An example of animating an element along a path using Motion for React. |
| `multifollow-pointer-with-spring` Drag with spring follow | motion, useMotionValue, useSpring, animate, drag | An example of making multiple elements follow the pointer on drag, using Moti... |
| `parallax` Parallax | motion, useScroll, useSpring, useTransform, animate | An example of creating a parallax effect using Motion for React's useScroll h... |
| `path-drawing` Path drawing | motion, animate, variants | An example of animating a SVG path to make a path drawing effect, using Motio... |
| `path-morphing` Path morphing | motion, useMotionValue, useTransform, animate | An example of creating smooth SVG path morphing animations with Motion for Re... |
| `reorder-items` Reorder animation | motion, layout | An example of animating when items are reordered, using Motion for React's la... |
| `rotate` Rotate | motion, animate | An example of animation the rotation of an element with Motion for React |
| `scroll-container` Element scroll-linked animation | motion, useMotionValue, useMotionValueEvent, useScroll, animate | An example of a scroll-linked animation using a specific element's scroll pos... |
| `scroll-linked` Scroll-linked animations | motion, useScroll | An example of linking an animation to scroll using Motion for React's useScro... |
| `scroll-linked-with-spring` Scroll-linked spring animation | motion, useSpring, useScroll | An example of linking an animation to scroll using Motion for React's useScro... |
| `scroll-track-element-in-viewport` Track element within viewport | motion, useScroll | An example of tracking an element within the viewport using Motion for React'... |
| `scroll-triggered` Scroll-triggered animations | motion, whileInView, viewport, variants | An example of triggering an animation when an element enters or leaves the vi... |
| `shared-layout-animation` Shared layout animation | AnimatePresence, motion, animate, layout, layoutId | An example of a shared layout animation, where two elements are animated from... |
| `state-updates` Animate state | motion, animate | An example of animating in response to state changes using Motion for React. |
| `transition` Transition options | motion, animate | An example of setting transition options in Motion for React. |
| `use-animation-frame` Spinning 3D cube | - | An example of using Motion for React's useAnimationFrame hook to animate a 3D... |
| `use-presence-data` usePresenceData | AnimatePresence, motion, animate, whileTap, whileFocus | An example of using usePresenceData to pass custom data through AnimatePresen... |
| `use-time` useTime | motion, useTransform | An example of using the useTime hook to animate an element at a constant speed. |
| `use-transform` Value transform & path drawing | motion, useMotionValue, useTransform, drag | An example of linking SVG path length to a drag gesture using Motion for Reac... |
| `variants` Variants | motion, animate, whileHover, whileTap, variants, layout | An example of orchestrating animation sequences using Motion for React's vari... |

### Category: Buttons

| Example | APIs | Description |
|---------|------|-------------|
| `hold-to-confirm` Hold to confirm | motion, useMotionValue, useTransform, animate | A Motion for React example showing long-press interaction. The button's event... |
| `material-design-ripple` Material Design: Ripple | AnimatePresence, motion, animate, whileHover | An example of creating a press ripple with Motion for React, inspired by Goog... |
| `multi-state-badge` Multi state badge | AnimatePresence, motion, useTransform, animate, layout | A badge that can be in multiple states, such as processing, success, or error. |

### Category: Carousel

| Example | APIs | Description |
|---------|------|-------------|
| `carousel` Carousel | motion, animate, drag | An example of creating a carousel using the Motion+ Carousel component for Re... |
| `carousel-autoplay` Carousel: Autoplay | motion, useMotionValue, animate, drag | An example of creating an autoplay carousel with a progress bar indicator usi... |
| `carousel-coverflow` Carousel: Coverflow | motion, useTransform, drag | An example of creating a coverflow-style carousel with 3D rotation using the ... |
| `carousel-free-scroll` Carousel: Free scroll | drag | An example of creating a free-scrolling carousel without snap points using th... |
| `carousel-item-offset` Carousel: Offset-linked animations | motion, useTransform, drag | An example of creating a carousel with offset-linked animations using the Mot... |
| `carousel-lightbox` Carousel: Lightbox | AnimatePresence, motion, animate, whileHover, whileTap, drag | An example of creating a carousel with lightbox overlay functionality using t... |
| `carousel-loop` Carousel: Loop | drag | An example of creating a carousel with infinite scrolling using the Motion+ C... |
| `carousel-pagination-arrows` Carousel: Pagination | motion, whileHover, whileTap, drag, layout | An example of creating a carousel with clickable pagination dots and navigati... |
| `carousel-pagination-page-count` Carousel: Pagination with page count | motion, whileHover, whileTap, drag | An example of creating a carousel with pagination indicators that show the cu... |
| `carousel-pagination-scaling` Carousel: Scaling pagination dots | motion, animate, whileHover, whileTap, drag, layout, layoutId | An example of creating a carousel with pagination dots that scale down at the... |
| `carousel-parallax` Carousel: Parallax | motion, useTransform, animate, drag | An example of creating a carousel with a parallax effectusing the Motion+ Car... |
| `carousel-progress-scrubber` Carousel: Progress scrubber | motion, useMotionValue, useMotionValueEvent, useTransform, drag | An example of creating a carousel with a progress scrubber using the Motion+ ... |
| `carousel-rtl` Carousel: RTL | motion, animate, drag | An example of creating a carousel with RTL support using the Motion+ Carousel... |
| `carousel-scale-items` Carousel: Scale items | motion, useTransform, animate, drag | An example of creating a carousel with items that scale out using the Motion+... |
| `carousel-thumbnail-gallery` Carousel: Thumbnail gallery | motion, animate, whileHover, whileTap, drag | An example of creating a carousel with a thumbnail navigation gallery using t... |
| `carousel-vertical` Carousel: Vertical | motion, whileHover, whileTap, drag | An example of creating a vertical carousel with navigation in Motion for React. |

### Category: Cursor

| Example | APIs | Description |
|---------|------|-------------|
| `cursor` Cursor: Adaptive caret size | Cursor, variants | An example of Motion+ Cursor with default settings. Motion+ Cursor is exclusi... |
| `cursor-custom-content` Cursor: Custom content | Cursor, motion, whileHover | An example of adding custom content to Motion+ Cursor when hovering over a sp... |
| `cursor-floating-target` Cursor: Floating target | Cursor, usePointerPosition, motion, useAnimate, useSpring, useTransform, animate, whileHover, whileTap, variants | An example of creating a floating target with rotating text and hover effects... |
| `cursor-follow` Cursor: Follow | Cursor, AnimatePresence, motion, variants | An example of making an element follow the cursor with a spring animation usi... |
| `cursor-hover-follow` Cursor: Image hover | Cursor, usePointerPosition, AnimatePresence, motion, useSpring, useTransform, useVelocity, variants, layout | An example of a cursor image hover effect using Motion for React. It links sk... |
| `cursor-magnetic` Cursor: Magnetic target | Cursor, motion, useMotionValue, animate, whileTap, variants, layout | An example of Motion+ Cursor with magnetic target snapping enabled. Motion+ C... |
| `cursor-multifollow` Cursor: Multi-follow | Cursor, variants | An example of attaching multiple elements to follow the cursor at the same ti... |
| `cursor-trail` Cursor trail | wrap, usePointerPosition, AnimatePresence, motion, useMotionValueEvent, useTransform, animate | An example of creating a cursor trail with images in Motion for React. |
| `cursor-trail-velocity` Cursor trail velocity | wrap, usePointerPosition, AnimatePresence, motion, useMotionValueEvent, useTransform, animate | An example of creating a cursor trail with velocity-based inertia animations ... |
| `ios-pointer` iOS pointer animation | Cursor, motion, whileTap, variants | An example of making the iOS pointer animation using Motion+ Cursor's magneti... |
| `line-graph` Line graph | AnimateNumber, Cursor, AnimatePresence, motion, MotionConfig, animate, variants | An SVG line graph that uses Cursor and AnimateNumber to display changes in da... |
| `magnetic-filings` Magnetic filings | usePointerPosition, motion, useTransform | An example of creating a grid of metal filings that rotate to point towards t... |

### Category: Dialog

| Example | APIs | Description |
|---------|------|-------------|
| `accordion` Accordion | motion, MotionConfig, animate, variants, layout, layoutId | An example of an accordion component using Motion for React. Animate accordio... |
| `family-dialog` Family-style dialog | AnimatePresence, motion, MotionConfig, animate, whileTap, layout, layoutId | Family-style dialog using Motion for React. It uses the HTML dialog and Motio... |
| `modal` Modal dialog | AnimatePresence, motion, animate, whileHover, whileTap | An example of a modal dialog using Motion for React and the HTML <dialog /> e... |
| `modal-shared-layout` Modal: Shared layout | AnimatePresence, motion, MotionConfig, useMotionValue, animate, layout, layoutId | A modal example demonstrating shared layout animations between a grid of item... |

### Category: Experimental

| Example | APIs | Description |
|---------|------|-------------|
| `apple-intelligence` Apple Intelligence ripple | animate | An example of the 'ripple' part of the Apple Intelligence animation using Mot... |
| `apple-watch-home-screen` Apple Watch Home Screen | motion, useMotionValue, useTransform, drag | A recreation of the Apple Watch home screen's iconic app grid with fluid drag... |
| `conic-gradient-pointer` Conic gradient pointer | motion, useMotionValue, useTransform, viewport | An example of creating a dynamic conic gradient with motion values and pointe... |
| `warp-overlay` Warp overlay | AnimatePresence, motion, useMotionValue, useTransform, animate, whileHover, whileTap | An example of creating a warp overlay effect with Motion for React. |

### Category: Forms

| Example | APIs | Description |
|---------|------|-------------|
| `carousel-ios-exposure-slider` Carousel: iOS exposure slider | motion, useMotionValue, useMotionValueEvent, useTransform, animate | An example of creating a camera exposure slider interface with animated notch... |
| `characters-remaining` Characters remaining | motion, useAnimate, animate | An example of creating a character counter with spring animations in Motion f... |
| `color-picker` Color picker | usePointerPosition, motion, useMotionValue, useSpring, useTransform, animate, whileHover, whileTap, variants | An example of creating a circular color picker with dots that react to cursor... |
| `ios-slider` iOS slider | motion, useMotionValue, useTransform, animate, whileFocus, drag | An example of recreating the iOS slider using Motion for React. This example ... |

### Category: Interactions

| Example | APIs | Description |
|---------|------|-------------|
| `image-reveal-slider` Image reveal slider | motion, useMotionValue, useTransform, animate, drag | An example of creating an interactive image comparison slider with Motion for... |
| `notifications-stack` iOS Notifications stack | motion, animate, whileHover, variants | A iOS inspired notifications stack using Motion for React's variants. |
| `swipe-actions` Swipe actions | motion, useMotionValue, useMotionValueEvent, useSpring, useTransform, animate, drag | iOS-style swipe actions where UI elements and animations respond dynamically ... |
| `tilt-card` Tilt card | motion, useSpring | A card that tilts based on pointer position using Motion for React's useSprin... |

### Category: Layout

| Example | APIs | Description |
|---------|------|-------------|
| `app-store` iOS App Store | AnimatePresence, motion, animate, layout, layoutId | An example of animating cards inspired by the iOS App Store using Motion for ... |
| `notifications-list` Notifications list | AnimatePresence, motion, animate, layout | An example of animating a list of animations as they're added and removed, us... |

### Category: Lists

| Example | APIs | Description |
|---------|------|-------------|
| `card-stack` Card stack | motion, useMotionValue, useTransform, animate, whileTap, drag | An example of creating a swipeable card stack with photos in Motion for React. |
| `infinite-loading` Infinite loading | motion, animate, onViewportEnter, variants | An example of creating an infinite loading list with staggered animations in ... |
| `staggered-grid` Physical stagger | motion, animate, variants | An example of animating elements with a delay staggered by physical distance. |
| `todo-list` To-do list | motion, useMotionValue, animate | A draggable to-do list with animated checkboxes and strikethrough effects usi... |

### Category: Loading

| Example | APIs | Description |
|---------|------|-------------|
| `loading-circle-spinner` Loading: Circle spinner | motion, animate | An example of creating a circle spinner loading animation with Motion for React. |
| `loading-fill-text` Fill text | motion, useSpring, useTransform | An example of creating a text fill loading animation with Motion for React. |
| `loading-jumping-dots` Loading: Jumping dots | motion, animate, variants | An example of creating a jumping dots loading animation with Motion for React. |
| `loading-line-reveal` Loading overlay | motion, useMotionValue, useMotionValueEvent, useSpring, useTransform, animate | An example of creating a line reveal loading animation with clipPath in Motio... |
| `loading-progress-bar` Loading progress bar | motion, useSpring | An example of creating a progress bar loading animation with Motion for React. |
| `loading-ripple` Loading ripple | motion, animate | An example of creating a ripple loading animation with Motion for React. |
| `loading-three-dots-pulse` Loading: Pulse dots | motion, animate, variants | An example of creating a pulsing dots loading animation with Motion for React. |

### Category: Navigation

| Example | APIs | Description |
|---------|------|-------------|
| `smooth-tabs` Smooth tabs | motion, useSpring, useTransform, useVelocity, whileFocus, layout, layoutId | A set of tabs that trigger a smooth transition between different views. |
| `tab-select` Tab select | motion, whileTap, whileFocus, layout, layoutId | An example of using making a tab select component with Motion for React's sha... |

### Category: Radix

| Example | APIs | Description |
|---------|------|-------------|
| `number-radix-slider` Radix: Slider with AnimateNumber | AnimateNumber, useMotionValue, useSpring, useVelocity | An example of using AnimateNumber and Radix Slider to animate numbers to labe... |
| `radix-accordion` Radix: Accordion | AnimatePresence, motion, MotionConfig, animate, whileTap, variants, layout, layoutId | An accordion component with smooth height animations using Radix UI and Motion |
| `radix-checkbox` Radix: Checkbox | motion, useMotionValue, useTransform, animate, whileHover, whileTap | A checkbox with path line animation for the tick using Radix UI and Motion |
| `radix-context-menu` Radix: Context Menu | motion, animate | A context menu with smooth animations using Radix UI and Motion. Note: This e... |
| `radix-dialog` Radix: Dialog | AnimatePresence, motion, animate, whileHover, whileTap | A dialog component with overlay and content animations using Radix UI and Motion |
| `radix-dropdown` Radix: Dropdown Menu | AnimatePresence, motion, animate, whileHover, whileTap, whileFocus | A dropdown menu with smooth animations using Radix UI and Motion |
| `radix-progress` Radix: Progress | motion, useMotionValue, useTransform, animate | An example of animating the Radix Progress component using Motion for React. |
| `radix-radio-group` Radix: Radio Group | AnimatePresence, motion, useMotionValue, useTransform, animate, whileHover, whileTap | An example of animating the Radix Radio Group component using Motion for React. |
| `radix-select` Radix: Select | motion, animate, whileHover, whileTap, viewport | An example of animating the Radix Select component using Motion for React. No... |
| `radix-switch` Radix: Switch | motion, animate, whileFocus, layout | An example of animating the Radix Switch component using Motion for React. Th... |
| `radix-tabs` Radix: Tabs | AnimatePresence, LayoutGroup, motion, animate, whileTap, layout, layoutId | An example of animating the Radix Tabs component using Motion for React. It u... |
| `radix-toast` Radix: Toast | AnimatePresence, motion, animate, whileTap, viewport, drag | An example of animating the Radix Toast component using Motion for React. It ... |
| `radix-toggle-group` Radix: Toggle Group | AnimatePresence, motion, animate, whileTap, whileFocus, layout, layoutId | An example of animating the Radix Toggle Group component using Motion for Rea... |
| `radix-toolbar` Radix: Toolbar | AnimatePresence, motion, animate, whileTap, whileFocus, layout, layoutId | An example of animating the Radix Toolbar component using Motion for React. T... |
| `radix-tooltip` Radix: Tooltip | AnimatePresence, motion, animate | An example of animating the Radix Tooltip component using Motion for React. |

### Category: Scroll

| Example | APIs | Description |
|---------|------|-------------|
| `scroll-highlight` Scroll highlight | motion, animate, onViewportEnter, viewport | An example of creating a scrollable list of items where the item in the cente... |

### Category: Studio SDK

| Example | APIs | Description |
|---------|------|-------------|
| `bezier-curve-editor` Bezier curve editor | motion, layout | Motion Studio SDK's bezier curve editor is a controlled, stylable React input... |

### Category: Text

| Example | APIs | Description |
|---------|------|-------------|
| `number-counter` Number counter | AnimateNumber, LayoutGroup, motion, whileHover, whileTap, whileFocus, layout | An example of using AnimateNumber to animate numbers. AnimateNumber is exclus... |
| `number-engagement-stats` Engagement stats | AnimateNumber, motion, animate, whileTap, variants | An example of using AnimateNumber to animate numbers on a social media engage... |
| `number-formatting` Number formatting | AnimateNumber, motion, animate, whileFocus, layout | An example of AnimateNumber's formatting options, including currency, roundin... |
| `number-price-switcher` Price switcher | AnimateNumber, motion, animate, layout, layoutId | An example of using AnimateNumber to animate a price between monthly and year... |
| `split-text` Split text | animate, stagger, splitText | An example of creating a text reveal animation with split text in Motion for ... |
| `split-text-scatter` Scatter text | animate, hover, splitText, useMotionValue | An example of creating a scatter text animation with split text in Motion for... |
| `split-text-wavy` Wavy text | animate, stagger, splitText | An example of creating a wavy text animation with split text in Motion for React |
| `text-reveal` Reveal text effect | motion, layout | An example of revealing text on hover using flexbox and Motion for React's la... |
| `typewriter` Typewriter | Typewriter | An example of a React typewriter animation using Motion for React's Typewrite... |
| `typewriter-change-content` Typewriter: Change Content | wrap, Typewriter | An example of a typewriter animation using Motion for React's Typewriter comp... |
| `typewriter-explode` Typewriter: Exploding Countdown | animate, mix, wrap, splitText, Typewriter | An example of a typewriter animation using Motion for React's Typewriter comp... |
| `typewriter-natural-typing` Typewriter: Natural Typing | wrap, Typewriter | An example of a React typewriter animation using Motion for React's Typewrite... |

### Category: Ticker

| Example | APIs | Description |
|---------|------|-------------|
| `ticker` Ticker | Ticker, motion, whileHover, variants | An example of an animated list of items using the Motion+ Ticker component fo... |
| `ticker-cursor` Ticker: Cursor | Cursor, Ticker, AnimatePresence, motion, variants | An example of creating a custom cursor that combines the Motion+ Ticker and C... |
| `ticker-draggable` Ticker: Draggable | Ticker, motion, useMotionValue, drag | An example of an draggable list of items using the Motion+ Ticker component f... |
| `ticker-keyboard-navigation` Ticker: Keyboard navigation | Ticker, motion, whileTap | An example of a Motion+ Ticker component for React containing focusable items... |
| `ticker-overflow` Ticker: Overflow | Ticker, motion, viewport, layout | An example of an animated list of items using the Motion+ Ticker component fo... |
| `ticker-rtl` Ticker: RTL | Ticker, motion, useTransform, layout | An example of Motion+ Ticker component for React with RTL support. |
| `ticker-scroll` Ticker: Scroll | Ticker, motion, useScroll, useTransform, onViewportEnter, viewport, drag | An example of a scroll-driven ticker animation Motion+ Ticker component for R... |
| `ticker-text-hover-effect` Ticker: Text hover effect | Ticker, AnimatePresence, motion, animate | An example of creating a text hover effect that replaces the label with a liv... |
| `ticker-use-ticker-item` Ticker: useTickerItem | Ticker, motion, useTransform | An example of animating ticker items based on their current offset using the ... |
| `ticker-y-axis` Ticker: Vertical scrolling | Ticker, drag | An example of a vertical scrolling ticker using the Motion+ Ticker component ... |

## Vue Examples (133)

### Category: Basics

| Example | APIs | Description |
|---------|------|-------------|
| `animate-presence-modes` AnimatePresence modes | AnimatePresence, motion, animate | An example of the three AnimatePresence modes (sync, wait, and popLayout) dem... |
| `announcement` Announcement | motion, animate, variants | The Motion for Vue announcement animation, built using Motion for Vue itself. |
| `aspect-ratio` Aspect Ratio | motion, layout | An example of animating aspect-ratio using Motion for Vue's layout animations. |
| `bounce-easing` Bounce Easing | motion, layout | An example of creating a bounce custom easing function in Motion for Vue. |
| `css-spring` CSS Spring | spring | An example of using Motion's spring function with Vue's CSS transition property. |
| `drag` Drag | motion, drag | An example of making an element draggable using Motion for Vue's drag prop. |
| `drag-constraints` Drag: Constraints | motion, drag | An example of making an element draggable using Motion for Vue's drag prop. Y... |
| `drag-lock-direction` Drag Lock Direction | motion, animate, drag | An example of locking the drag direction of a Motion for Vue component. |
| `enter-animation` Enter Animation | motion, animate | An example of animating an element when it's added to the DOM using Motion fo... |
| `exit-animation` Exit animation | AnimatePresence, motion, animate, whilePress | An example of animating an element when it's removed from the DOM using Anima... |
| `follow-pointer-with-spring` Follow pointer with spring | motion, useMotionValue, useSpring | An example of making an element follow the pointer with a spring using Motion... |
| `gestures` Gestures | motion, whileHover, whilePress | An example of using gestures to animate an element using Motion for Vue's sim... |
| `html-content` HTML content | animate, useMotionValue, useTransform | An example using Motion for Vue to animate HTML content by rendering a Motion... |
| `keyframes` Keyframes | motion, animate | An example of animating an element using keyframes with Motion for Vue. |
| `keyframes-wildcards` Keyframe wildcards | motion, whileHover | An example of using Motion for Vue's keyframe wildcards to create interruptib... |
| `layout-animation` Layout animation | motion, layout | An example of animating a layout change using Motion for Vue's simple layout ... |
| `motion-path` Motion along a path | motion, animate | An example of animating an element along a path using Motion for Vue. |
| `multifollow-pointer-with-spring` Drag with spring follow | motion, useMotionValue, useSpring, animate, drag | An example of making multiple elements follow the pointer on drag, using Moti... |
| `parallax` Parallax | motion, useScroll, useSpring, useTransform, animate | An example of creating a parallax effect using Motion for Vue's useScroll hook. |
| `path-drawing` Path drawing | motion, animate, variants | An example of animating a SVG path to make a path drawing effect, using Motio... |
| `path-morphing` Path morphing | motion, useTransform, animate | An example of morphing one SVG path to another path, using Motion for Vue and... |
| `reorder-items` Reorder animation | motion, layout | An example of animating when items are reordered, using Motion for Vue's layo... |
| `rotate` Rotate | motion, animate | An example of animation the rotation of an element with Motion for Vue |
| `scale-correction` Scale correction | motion, layout | An example of layout animation with scale correction using Motion for Vue. |
| `scroll-container` Element scroll-linked animation | motion, useScroll, useMotionValue, useMotionValueEvent, animate | An example of a scroll-linked animation using a specific element's scroll pos... |
| `scroll-linked` Scroll-linked animations | motion, useScroll | An example of linking an animation to scroll using Motion for Vue's useScroll... |
| `scroll-linked-with-spring` Scroll-linked spring animation | motion, useScroll, useSpring | An example of linking an animation to scroll using Motion for Vue's useScroll... |
| `scroll-track-element-in-viewport` Track element within viewport | motion, useScroll | An example of tracking an element within the viewport using Motion for Vue's ... |
| `scroll-triggered` Scroll-triggered animations | motion, whileInView, variants | An example of triggering an animation when an element enters or leaves the vi... |
| `shared-layout-animation` Shared layout animation | motion, AnimatePresence, animate, layout, layoutId | An example of a shared layout animation, where two elements are animated from... |
| `state-updates` Animate state | motion, animate | An example of animating in response to state changes using Motion for Vue. |
| `transition` Transition | motion, animate | An example of setting transition options in Motion for Vue. |
| `use-animation-frame` useAnimationFrame | - | An example of using Motion for Vue's useAnimationFrame hook to animate a 3D c... |
| `use-time` useTime | motion, useTransform | An example of using the useTime hook to animate an element at a constant speed. |
| `use-transform` Value transform & path drawing | useMotionValue, useTransform, motion, drag | An example of linking SVG path length to a drag gesture using Motion for Vue'... |
| `variants` Variants | motion, animate, whileHover, whilePress, variants | An example of orchestrating animation sequences using Motion for Vue's variants. |

### Category: Buttons

| Example | APIs | Description |
|---------|------|-------------|
| `hold-to-confirm` Hold to confirm | animate, motion, useMotionValue, useTransform | A Motion for Vue example showing long-press interaction. The button's events ... |
| `material-design-ripple` Material Design: Ripple | motion, AnimatePresence, animate, whileHover | An example of creating a press ripple with Motion for Vue, inspired by Google... |
| `multi-state-badge` Multi state badge | animate, useTransform, motion, AnimatePresence | A badge that can be in multiple states, such as processing, success, or error. |

### Category: Carousel

| Example | APIs | Description |
|---------|------|-------------|
| `carousel` Carousel | motion, animate, drag | An example of creating a carousel using the Motion+ Carousel component for Re... |
| `carousel-autoplay` Carousel: Autoplay | animate, motion, useMotionValue, drag | An example of creating an autoplay carousel with a progress bar indicator usi... |
| `carousel-coverflow` Carousel: Coverflow | motion, useTransform, drag | An example of creating a coverflow-style carousel with 3D rotation using the ... |
| `carousel-free-scroll` Carousel: Free scroll | motion, drag | An example of creating a free-scrolling carousel without snap points using th... |
| `carousel-item-offset` Carousel: Offset-linked animations | motion, useTransform, drag | An example of creating a carousel with offset-linked animations using the Mot... |
| `carousel-lightbox` Carousel: Lightbox | motion, AnimatePresence, animate, whileHover, whilePress, variants, drag | An example of creating a carousel with lightbox overlay functionality using t... |
| `carousel-loop` Carousel: Loop | drag | An example of creating a carousel with infinite scrolling using the Motion+ C... |
| `carousel-pagination-arrows` Carousel: Pagination | motion, LayoutGroup, whileHover, whilePress, drag, layout | An example of creating a carousel with clickable pagination dots and navigati... |
| `carousel-pagination-page-count` Carousel: Pagination with page count | motion, whileHover, whilePress, drag | An example of creating a carousel with pagination indicators that show the cu... |
| `carousel-pagination-scaling` Carousel: Scaling pagination dots | motion, LayoutGroup, animate, whileHover, whilePress, drag, layout, layoutId | An example of creating a carousel with pagination dots that scale down at the... |
| `carousel-parallax` Carousel: Parallax | motion, useTransform, animate, drag | An example of creating a carousel with a parallax effectusing the Motion+ Car... |
| `carousel-progress-scrubber` Carousel: Progress scrubber | motion, useTransform, useMotionValue, useMotionValueEvent, drag | An example of creating a carousel with a progress scrubber using the Motion+ ... |
| `carousel-rtl` Carousel: Vertical | motion, animate, drag | An example of creating a vertical carousel with navigation in Motion for Vue. |
| `carousel-thumbnail-gallery` Carousel: Thumbnail gallery | motion, animate, whileHover, whilePress, drag | An example of creating a carousel with a thumbnail navigation gallery using t... |
| `carousel-vertical` Carousel: Vertical | motion, whileHover, whilePress | An example of creating a vertical carousel with navigation in Motion for Vue. |

### Category: Cursor

| Example | APIs | Description |
|---------|------|-------------|
| `cursor` Cursor: Adaptive caret size | Cursor, variants | An example of Motion+ Cursor with default settings. Motion+ Cursor is exclusi... |
| `cursor-custom-content` Cursor: Custom content | motion, Cursor, whileHover | An example of adding custom content to Motion+ Cursor when hovering over a sp... |
| `cursor-floating-target` Cursor: Floating target | Cursor, motion, transform, useAnimate, useSpring, useTransform, animate, whileHover, whilePress, variants | An example of creating a floating target with rotating text and hover effects... |
| `cursor-follow` Cursor: Follow | motion, Cursor, variants | An example of making an element follow the cursor with a spring animation usi... |
| `cursor-hover-follow` Cursor: Image hover | Cursor, useSpring, useTransform, useVelocity, motion, variants, layout | An example of a cursor image hover effect using Motion for Vue. It links skew... |
| `cursor-magnetic` Cursor: Magnetic target | Cursor, animate, motion, useMotionValue, whilePress, variants, layout | An example of Motion+ Cursor with magnetic target snapping enabled. Motion+ C... |
| `cursor-multifollow` Cursor: Multi-follow | Cursor, variants | An example of attaching multiple elements to follow the cursor at the same ti... |
| `cursor-trail` Cursor trail | motion, useMotionValueEvent, useTransform, AnimatePresence, animate | An example of creating a cursor trail with images in Motion for Vue. |
| `cursor-trail-velocity` Cursor trail velocity | motion, useMotionValueEvent, useTransform, AnimatePresence, animate | An example of creating a cursor trail with velocity-based inertia animations ... |
| `ios-pointer` iOS pointer animation | Cursor, motion, whilePress, variants | An example of making the iOS pointer animation using Motion+ Cursor's magneti... |
| `line-graph` Line graph | motion, AnimateNumber, Cursor, animate, variants | An SVG line graph that uses Cursor and AnimateNumber to display changes in da... |
| `magnetic-filings` Magnetic filings | useTransform | An example of creating a grid of metal filings that rotate to point towards t... |

### Category: Dialog

| Example | APIs | Description |
|---------|------|-------------|
| `accordion` Accordion | motion, MotionConfig, animate, whilePress, variants, layout, layoutId | An example of an accordion component using Motion for Vue. Animate accordion ... |
| `family-dialog` Family-style dialog | AnimatePresence, MotionConfig, motion, animate, whilePress, variants, layout, layoutId | Family-style dialog using Motion for Vue. It uses the HTML dialog and Motion'... |
| `modal` Modal dialog | motion, AnimatePresence, animate, whileHover, whilePress | An example of a modal dialog using Motion for Vue and the HTML <dialog /> ele... |
| `modal-shared-layout` Modal: Shared layout | AnimatePresence, motion, MotionConfig, useMotionValue, animate, layout, layoutId | A modal example demonstrating shared layout animations between a grid of item... |

### Category: Experimental

| Example | APIs | Description |
|---------|------|-------------|
| `apple-intelligence` Apple Intelligence ripple | motion, animate, whileHover, whilePress | An example of the 'ripple' part of the Apple Intelligence animation using Mot... |
| `apple-watch-home-screen` Apple Watch Home Screen | motion, useMotionValue, useTransform, drag | A recreation of the Apple Watch home screen's iconic app grid with fluid drag... |
| `color-picker` Color picker | useSpring, useTransform, motion, useMotionValue, animate, whileHover, whilePress, variants | An example of creating a circular color picker with dots that react to cursor... |
| `conic-gradient-pointer` Conic gradient pointer | motion, useMotionValue, useTransform | An example of creating a dynamic conic gradient with motion values and pointe... |
| `warp-overlay` Warp overlay | animate, AnimatePresence, motion, useMotionValue, useTransform, whileHover, whilePress, variants | An example of creating a warp overlay effect with Motion for Vue. |

### Category: Forms

| Example | APIs | Description |
|---------|------|-------------|
| `characters-remaining` Characters remaining | motion, transform, useAnimate, animate | An example of creating a character counter with spring animations in Motion f... |
| `ios-slider` iOS slider | animate, motion, mix, useMotionValue, useTransform, whileFocus, drag | An example of recreating the iOS slider using Motion for Vue. This example us... |

### Category: Interactions

| Example | APIs | Description |
|---------|------|-------------|
| `image-reveal-slider` Image reveal slider | animate, motion, useMotionValue, useTransform, drag | An image reveal slider that uses Motion for Vue's drag functionality and useT... |
| `notifications-stack` iOS Notifications stack | motion, animate, variants | A iOS inspired notifications stack using Motion for Vue's variants. |
| `swipe-actions` Swipe actions | animate, motion, useMotionValue, useMotionValueEvent, useSpring, useTransform | iOS-style swipe actions where UI elements and animations respond dynamically ... |
| `tilt-card` Tilt card | motion, useSpring | A card that tilts based on pointer position using Motion for Vue's useSpring ... |

### Category: Layout

| Example | APIs | Description |
|---------|------|-------------|
| `app-store` iOS App Store | AnimatePresence, motion, animate, layout, layoutId | An example of animating cards inspired by the iOS App Store using Motion for ... |
| `notifications-list` Notifications list | motion, AnimatePresence, animate, layout | An example of animating a list of animations as they're added and removed, us... |

### Category: Lists

| Example | APIs | Description |
|---------|------|-------------|
| `card-stack` Card stack | animate, mix, motion, transform, whilePress, drag | An example of creating a swipeable card stack with photos in Motion for Vue. |
| `infinite-loading` Infinite loading | motion, stagger, animate, variants | An example of creating an infinite loading list with staggered animations in ... |
| `staggered-grid` Physical stagger | motion, animate, variants | An example of animating elements with a delay staggered by physical distance. |
| `todo-list` To-do list | LayoutGroup, motion, useMotionValue, animate | A draggable to-do list with animated checkboxes and strikethrough effects usi... |

### Category: Loading

| Example | APIs | Description |
|---------|------|-------------|
| `loading-circle-spinner` Loading: Circle spinner | motion, animate | An example of creating a circle spinner loading animation with Motion for Vue. |
| `loading-fill-text` Fill text | motion, useSpring, useTransform | An example of creating a text fill loading animation with Motion for Vue. |
| `loading-jumping-dots` Loading: Jumping dots | motion, animate, variants | An example of creating a jumping dots loading animation with Motion for Vue. |
| `loading-line-reveal` Loading overlay | motion, animate, useSpring, useTransform, useMotionValue | An example of creating a line reveal loading animation with clipPath in Motio... |
| `loading-progress-bar` Loading progress bar | motion, useSpring | An example of creating a progress bar loading animation with Motion for Vue. |
| `loading-ripple` Loading ripple | motion, animate | An example of creating a ripple loading animation with Motion for Vue. |
| `loading-three-dots-pulse` Loading: Pulse dots | motion, animate, variants | An example of creating a pulsing dots loading animation with Motion for Vue. |

### Category: Navigation

| Example | APIs | Description |
|---------|------|-------------|
| `smooth-tabs` Smooth tabs | motion, useSpring, useTransform, useVelocity, useMotionValue, whileFocus, layout, layoutId | A set of tabs that trigger a smooth transition between different views. |
| `tab-select` Tab select | motion, whilePress, whileFocus, layout, layoutId | An example of using making a tab select component with Motion for Vue's share... |

### Category: Radix

| Example | APIs | Description |
|---------|------|-------------|
| `number-radix-slider` Radix: Slider with AnimateNumber | useMotionValue, useSpring, useVelocity, AnimateNumber | An example of using AnimateNumber and Radix Slider to animate numbers to labe... |
| `radix-accordion` Radix: Accordion | motion, AnimatePresence, MotionConfig, animate, whilePress, variants, layout, layoutId | An accordion component with smooth height animations using Radix UI and Motion |
| `radix-checkbox` Radix: Checkbox | motion, useMotionValue, useTransform, animate, whileHover, whilePress | A checkbox with path line animation for the tick using Radix UI and Motion |
| `radix-context-menu` Radix: Context Menu | AnimatePresence, motion, animate | A context menu with smooth animations using Radix UI and Motion.  |
| `radix-dialog` Radix: Dialog | AnimatePresence, motion, animate, whileHover, whilePress | A dialog component with overlay and content animations using Radix-vue and Mo... |
| `radix-dropdown` Radix: Dropdown Menu | motion, AnimatePresence, animate, whileHover, whilePress, whileFocus | A dropdown menu with smooth animations using Radix UI and Motion |
| `radix-progress` Radix: Progress | motion, useMotionValue, useTransform, animate | An example of animating the Radix Progress component using Motion for Vue. |
| `radix-radio-group` Radix: Radio Group | motion, AnimatePresence, useMotionValue, useTransform, animate, whileHover, whilePress, variants | An example of animating the Radix Radio Group component using Motion for Vue. |
| `radix-select` Radix: Select | motion, AnimatePresence, animate, whileHover, whilePress | An example of animating the Radix Select component using Motion for Vue. |
| `radix-switch` Radix: Switch | motion, animate, whileFocus, layout | An example of animating the Radix Switch component using Motion for Vue. This... |
| `radix-tabs` Radix: Tabs | LayoutGroup, motion, AnimatePresence, animate, whilePress, layout, layoutId | An example of animating the Radix Tabs component using Motion for Vue. It use... |
| `radix-toast` Radix: Toast | motion, AnimatePresence, animate, whilePress, drag | An example of animating the Radix Toast component using Motion for Vue. |
| `radix-toggle-group` Radix: Toggle Group | motion, AnimatePresence, animate, whilePress, whileFocus, layout, layoutId | An example of animating the Radix Toggle Group component using Motion for Vue... |
| `radix-toolbar` Radix: Toolbar | motion, AnimatePresence, animate, whilePress, whileFocus, layout, layoutId | An example of animating the Radix Toolbar component using Motion for Vue. The... |
| `radix-tooltip` Radix: Tooltip | AnimatePresence, motion, animate | An example of animating the Radix Tooltip component using Motion for Vue. |

### Category: Scroll

| Example | APIs | Description |
|---------|------|-------------|
| `scroll-highlight` Scroll highlight | motion, animate | An example of creating a scrollable list of items where the item in the cente... |

### Category: Text

| Example | APIs | Description |
|---------|------|-------------|
| `number-counter` Number counter | motion, LayoutGroup, AnimateNumber, layout | An example of using AnimateNumber to animate numbers. AnimateNumber is exclus... |
| `number-engagement-stats` Engagement stats | AnimateNumber, mix, motion, animate, variants | An example of using AnimateNumber to animate numbers on a social media engage... |
| `number-formatting` Number formatting | AnimateNumber, motion, animate, layout | An example of AnimateNumber's formatting options, including currency, roundin... |
| `number-price-switcher` Price switcher | AnimateNumber, motion, animate, layout, layoutId | An example of using AnimateNumber to animate a price between monthly and year... |
| `split-text` Split text | animate, stagger, splitText | An example of creating a text reveal animation with split text in Motion for Vue |
| `split-text-wavy` Wavy text | animate, stagger, splitText | An example of creating a wavy text animation with split text in Motion for Vue |
| `text-reveal` Reveal text effect | motion, layout | An example of revealing text on hover using flexbox and Motion for Vue's layo... |
| `typewriter` Typewriter | - | An example of a typewriter effect using Motion for Vue's Typewriter component. |
| `typewriter-change-content` Typewriter: Change Content | animate | An example of a typewriter animation using Motion for Vue's Typewriter compon... |
| `typewriter-explode` Typewriter: Exploding Countdown | animate, mix, splitText | An example of a typewriter animation using Motion for Vue's Typewriter compon... |
| `typewriter-natural-typing` Typewriter: Natural Typing | - | An example of a Vue typewriter animation using Motion for Vue's Typewriter co... |

### Category: Ticker

| Example | APIs | Description |
|---------|------|-------------|
| `ticker` Ticker | motion, whileHover, variants | An example of an animated list of items using the Motion+ Ticker component fo... |
| `ticker-cursor` Ticker: Cursor | Cursor, motion, variants | An example of creating a custom cursor that combines the Motion+ Ticker and C... |
| `ticker-draggable` Ticker: Draggable | motion, useMotionValue, drag | An example of an draggable list of items using the Motion+ Ticker component f... |
| `ticker-keyboard-navigation` Ticker: Keyboard navigation | motion, whilePress | An example of a Motion+ Ticker component for Vue containing focusable items. ... |
| `ticker-overflow` Ticker: Overflow | motion, layout | An example of an animated list of items using the Motion+ Ticker component fo... |
| `ticker-rtl` Ticker: RTL | motion, useTransform, layout | An example of Motion+ Ticker component for Vue with RTL support. |
| `ticker-scroll` Ticker: Scroll | motion, useScroll, useTransform, whileInView, drag | An example of a scroll-driven ticker animation Motion+ Ticker component for Vue. |
| `ticker-text-hover-effect` Ticker: Text hover effect | AnimatePresence, motion, animate | An example of creating a text hover effect that replaces the label with a liv... |
| `ticker-use-ticker-item` Ticker: useTickerItem | motion, useTransform | An example of animating ticker items based on their current offset using the ... |
| `ticker-y-axis` Ticker: Vertical scrolling | drag | An example of a vertical scrolling ticker using the Motion+ Ticker component ... |

## Quick Reference

### Fetch Example Code
```bash
# JavaScript example
./scripts/get-example.sh accordion js

# React example
./scripts/get-example.sh tabs react

# Vue example
./scripts/get-example.sh carousel vue
```

### Browse by Category

- **Base UI**
- **Basics**
- **Buttons**
- **Carousel**
- **Cursor**
- **Dialog**
- **Experimental**
- **Forms**
- **Interactions**
- **Layout**
- **Lists**
- **Loading**
- **Navigation**
- **Radix**
- **Scroll**
- **Studio SDK**
- **Text**
- **Ticker**
